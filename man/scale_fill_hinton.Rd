% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale-hinton.R
\name{scale_fill_hinton}
\alias{scale_fill_hinton}
\title{Colour scale for Hinton diagrams}
\usage{
scale_fill_hinton(..., values = NULL, guide = "none")
}
\arguments{
\item{...}{Additional arguments passed on to \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}}.}

\item{values}{Named character vector of colours for \code{"positive"},
\code{"negative"}, and \code{"unsigned"} fill values.  Override individual colours
by passing a partial named vector, e.g.
\code{values = c(negative = "grey50")} merges with the defaults.}

\item{guide}{Legend guide.  Defaults to \code{"none"} (no legend).}
}
\value{
A ggplot2 scale object.
}
\description{
Maps the sign-encoding produced by \code{\link[=stat_hinton]{stat_hinton()}} to the conventional
Hinton colour scheme: white for positive values, black for negative values.
For unsigned data (all non-negative), all squares are drawn in black.
}
\details{
This scale is a thin wrapper around \code{\link[ggplot2:scale_manual]{ggplot2::scale_fill_manual()}} with the default
legend suppressed.  Pass \code{guide = "legend"} to restore the legend, or
override the \code{values} argument to use custom colours.
}
\examples{
library(ggplot2)
m <- matrix(c(0.8, -0.3, 0.5, -0.9, 0.1, 0.6), 2, 3)
df <- matrix_to_hinton(m)

ggplot(df, aes(x = col, y = row, weight = weight)) +
  geom_hinton() +
  scale_fill_hinton() +
  theme_hinton()

}
