% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-matrix.R
\name{matrix_to_hinton}
\alias{matrix_to_hinton}
\title{Convert a matrix to a tidy data frame for use with \code{geom_hinton()}}
\usage{
matrix_to_hinton(
  x,
  rowname_col = "row",
  colname_col = "col",
  value_col = "weight"
)
}
\arguments{
\item{x}{A numeric matrix.}

\item{rowname_col}{Name of the column that will hold the row index.
Default \code{"row"}.}

\item{colname_col}{Name of the column that will hold the column index.
Default \code{"col"}.}

\item{value_col}{Name of the column that will hold the matrix values.
Default \code{"weight"}.}
}
\value{
A data frame with columns named by \code{rowname_col}, \code{colname_col},
and \code{value_col}.  If \code{x} has row or column names, additional columns
\code{row_label} and \code{col_label} are included.
}
\description{
Reshapes a numeric matrix into a long-form data frame with one row per
matrix entry.  Row 1 of the matrix is placed at the \emph{top} of the resulting
plot (highest y value), matching the visual convention of matrix notation.
}
\examples{
m <- matrix(c(0.8, -0.3, 0.5, -0.9, 0.1, 0.6), nrow = 2)
matrix_to_hinton(m)

# Named matrix
rownames(m) <- c("a", "b")
colnames(m) <- c("x", "y", "z")
matrix_to_hinton(m)

}
