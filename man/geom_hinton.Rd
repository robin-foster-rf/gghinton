% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom-hinton.R, R/stat-hinton.R
\docType{data}
\name{GeomHinton}
\alias{GeomHinton}
\alias{StatHinton}
\alias{geom_hinton}
\alias{stat_hinton}
\title{Hinton diagrams for ggplot2}
\usage{
geom_hinton(
  mapping = NULL,
  data = NULL,
  stat = "hinton",
  position = "identity",
  ...,
  scale_by = c("panel", "global"),
  background = TRUE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)

stat_hinton(
  mapping = NULL,
  data = NULL,
  geom = "hinton",
  position = "identity",
  ...,
  scale_by = c("panel", "global"),
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{The data to be displayed in this layer.}

\item{stat}{The statistical transformation to use.  For \code{geom_hinton()} the
default is \code{"hinton"}.}

\item{position}{Position adjustment.}

\item{...}{Other arguments passed on to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{scale_by}{\code{"panel"} (default) normalises each panel independently so
the largest value in a panel fills its cell.  \code{"global"} uses the
largest value across all panels, enabling cross-panel magnitude comparison.}

\item{background}{Logical.  Draw a grey background rectangle for signed
data?  Default \code{TRUE}.  Set to \code{FALSE} to suppress the background.}

\item{na.rm}{If \code{TRUE}, rows where \code{weight} is \code{NA} are silently dropped
before computing square sizes.  If \code{FALSE} (default), they are dropped
without a warning (ggplot2 will not render rectangles whose required
aesthetics are \code{NA}).}

\item{show.legend}{Logical.  Should this layer be included in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics rather than
combining with them.}

\item{geom}{The geometric object to use when drawing.  For \code{stat_hinton()}
the default is \code{"hinton"}.}
}
\value{
A ggplot2 layer that can be added to a \code{\link[ggplot2:ggplot]{ggplot2::ggplot()}} object.
}
\description{
\code{geom_hinton()} draws a Hinton diagram: a grid of squares whose area is
proportional to the magnitude of each value.  For signed data, positive
values are shown as white squares and negative values as black squares on
a grey background.  For unsigned (non-negative) data the background is
omitted and squares are drawn in black.
}
\section{Aesthetics}{

\code{geom_hinton()} understands the following aesthetics (required aesthetics
are in \strong{bold}):
\itemize{
\item \strong{\code{x}}: column position (numeric or factor)
\item \strong{\code{y}}: row position (numeric or factor)
\item \strong{\code{weight}}: the value to display; determines square size and colour
\item \code{alpha}
\item \code{colour} (border colour; \code{NA} by default, no border)
\item \code{fill} (overrides the automatic sign-based colour)
\item \code{linewidth}
\item \code{linetype}
}
}

\section{Computed variables}{

\code{stat_hinton()} adds the following columns to the data:

\describe{
\item{\code{xmin}, \code{xmax}, \code{ymin}, \code{ymax}}{Rectangle bounds for each square.}
\item{\code{fill}}{\code{"positive"}, \code{"negative"}, or \code{"unsigned"}.}
\item{\code{hinton_signed}}{Logical; \code{TRUE} when the panel contains any negative
values.  Read by \code{GeomHinton} to decide whether to draw the grey
background (after \code{scale_fill_hinton()} has already replaced the fill
labels, making \code{fill == "negative"} checks unreliable).}
\item{\code{cell_w}, \code{cell_h}}{Inferred cell spacing used to size the background.}
}
}

\section{Aspect ratio}{

For squares to appear as squares on screen, add \code{coord_fixed()} to your
plot.  Without it, the cells may appear rectangular if the plot's x and y
axes have different scales.
}

\examples{
library(ggplot2)
m <- matrix(c(0.8, -0.3, 0.5, -0.9, 0.1, 0.6, 0.4, -0.7, 0.2), 3, 3)
df <- matrix_to_hinton(m)

ggplot(df, aes(x = col, y = row, weight = weight)) +
  geom_hinton() +
  scale_fill_hinton() +
  coord_fixed() +
  theme_hinton()

}
\keyword{datasets}
